# RI-0 HSK local configuration
DATABASE_URL=postgres://consent:consent_password@localhost:5432/consent_ledger
RUST_LOG=info
PORT=8080
HSK_API_BASE_URL=http://localhost:8080/v1
HSK_DEMO_AUDIT_PATH=reports/demo_audit.json
HSK_REQUIRE_AUTH=false
# Never commit real secrets. Set these in your deployment secret manager.
JWT_SECRET=change-me-local-only
API_KEY=change-me-local-only
